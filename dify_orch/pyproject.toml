[project]
name = "dify-orch"
version = "0.1.0"
description = "Dify Orchestrator Client"
requires-python = ">=3.8"
dependencies = [
    "dify-client>=0.1.0",
    "python-dotenv>=1.0.0",
    "requests>=2.25.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"